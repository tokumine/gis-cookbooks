server {
  listen   80;
  server_name  localhost;

  access_log  <%= @node[:nginx][:log_dir] %>/localhost.access.log;
	root <%= @node[:web][:dir] %>/<%= @node[:web][:vhosts] %>/<%= @node[:web][:default_site] %>/public;
  passenger_enabled on;	
}       