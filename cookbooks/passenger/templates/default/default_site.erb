server {
  listen   80;
  server_name  localhost;

  access_log  <%= @node[:nginx][:log_dir] %>/localhost.access.log;

  location / {
    root   <%= @node[:web][:dir] %>/<%= @node[:web][:vhosts] %>/<%= @node[:web][:default_site] %>;
    index  index.html index.htm;
  }
}
